<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>内存 on Q-song | 技术.生活.阅读.思考</title>
    <link>https://MU-GONG-SONG.github.io/blog/tags/%E5%86%85%E5%AD%98/</link>
    <description>Recent content in 内存 on Q-song | 技术.生活.阅读.思考</description>
    <generator>Hugo -- 0.152.2</generator>
    <language>zh</language>
    <lastBuildDate>Tue, 14 Oct 2025 00:00:00 +0000</lastBuildDate>
    <atom:link href="https://MU-GONG-SONG.github.io/blog/tags/%E5%86%85%E5%AD%98/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>你了解内存管理吗??</title>
      <link>https://MU-GONG-SONG.github.io/blog/posts/memory_management/</link>
      <pubDate>Tue, 14 Oct 2025 00:00:00 +0000</pubDate>
      <guid>https://MU-GONG-SONG.github.io/blog/posts/memory_management/</guid>
      <description>&lt;blockquote&gt;
&lt;p&gt;栈分配和堆分配确实都发生在“对象分配器”的分配阶段，
但它们走的是不同的路径：
栈分配走的是编译期静态分配；
堆分配走的是运行时对象分配器（runtime.mallocgc）。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&#34;1程序启动阶段&#34;&gt;1.程序启动阶段&lt;/h3&gt;
&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt; Go 运行时启动时（runtime 初始化），会：
向操作系统申请一大块虚拟内存（称为 arena）；
由 页分配器（page allocator） 管理这块内存；
构建堆内存管理结构（mheap、mcentral、mcache）。
&amp;gt; 这部分只是“预留”内存，真正的对象分配还没发生。
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;2用户程序触发分配对象分配阶段&#34;&gt;2.用户程序触发分配（对象分配阶段）&lt;/h3&gt;
&lt;p&gt;当用户代码中创建变量时，比如：&lt;/p&gt;
&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;x := MyStruct{}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;编译器会在编译阶段决定这个对象是：
分配在栈上（stack allocation）
还是分配在堆上（heap allocation）
这个决策是通过 逃逸分析（Escape Analysis） 完成的。&lt;/p&gt;
&lt;h3 id=&#34;3栈分配的过程&#34;&gt;3.栈分配的过程&lt;/h3&gt;
&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;如果编译器认为对象只在当前函数作用域内使用，不会被外部引用：
这个对象会直接分配在栈上；
不会调用运行时的内存分配器；
栈内存是随函数调用帧自动增长/释放的；
GC 不需要扫描或回收它。
&amp;gt; 关键：栈分配是编译期确定的，性能最好。
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;4堆分配的过程&#34;&gt;4.堆分配的过程&lt;/h3&gt;
&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;如果对象被闭包引用、返回地址或传递给其他 goroutine，则会发生逃逸：
编译器在生成代码时，会调用运行时的分配器 runtime.mallocgc；
mallocgc 会从当前 P 的 mcache 尝试获取一个合适的 span；
若 mcache 缓存不足，就从 mcentral → mheap 逐层申请；
分配完成后，GC 会在堆上追踪这个对象。
&amp;gt;关键：堆分配是运行时动态完成的，涉及 GC 管理。
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;5回收阶段&#34;&gt;5.回收阶段&lt;/h3&gt;
&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;当对象不再被引用时，GC 会标记并清除；
被清除的内存重新回收到 mcache / mcentral / mheap；
长期未使用的页可能由scavenger（拾荒器）归还给 OS。
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;6对比栈和堆分配&#34;&gt;6.对比栈和堆分配&lt;/h3&gt;
&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th&gt;类型&lt;/th&gt;
          &lt;th&gt;分配阶段&lt;/th&gt;
          &lt;th&gt;分配位置&lt;/th&gt;
          &lt;th&gt;分配速度&lt;/th&gt;
          &lt;th&gt;是否由 GC 管理&lt;/th&gt;
          &lt;th&gt;是否逃逸&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td&gt;栈分配&lt;/td&gt;
          &lt;td&gt;编译期（静态）&lt;/td&gt;
          &lt;td&gt;每个 goroutine 的调用栈&lt;/td&gt;
          &lt;td&gt;极快&lt;/td&gt;
          &lt;td&gt;否&lt;/td&gt;
          &lt;td&gt;否&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td&gt;堆分配&lt;/td&gt;
          &lt;td&gt;运行时（动态）&lt;/td&gt;
          &lt;td&gt;运行时堆（mheap）&lt;/td&gt;
          &lt;td&gt;慢&lt;/td&gt;
          &lt;td&gt;是&lt;/td&gt;
          &lt;td&gt;是&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;</description>
    </item>
  </channel>
</rss>
